data:
  # variant: workstation  # default value, can be overridden by env var (export CHEZMOI_DATA_variant=wsl)
  variants:
    workstation:
      install:
        starship: true
        pnpm: true
        uv: true
    wsl:
      install:
        starship: true
        pnpm: true
        uv: true
    devcontainer:
      install:
        starship: true
        pnpm: false
        uv: false
