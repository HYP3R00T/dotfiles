---
icon: lucide/rocket
---

# Documentation

This repository's documentation is intentionally minimal.

Use the hosted installer when you want the published bootstrap entrypoint:

```bash
bash <(curl -fsSL https://dotfiles.hyperoot.dev/setup.sh) [variant]
```

- See [Install & Quickstart](install.md) for self-contained installer and DevContainer instructions.
